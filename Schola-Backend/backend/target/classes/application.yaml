spring:
  application:
    name: backend

  datasource:
    url:      jdbc:postgresql://localhost:5432/schola
    username: postgres
    password: cape123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  security:
    user:
      name: admin
      password: admin

server:
  port: 8080

jwt:

  secret: cape123cape1234cape12345cape123456
  expiration: 86400000